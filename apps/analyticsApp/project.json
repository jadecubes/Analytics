{
  "name": "analyticsApp",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/analyticsApp/src",
  "projectType": "application",
  "targets": {
    "w-build": {
      "options": {
        "commands": [
          {
            "command": "NODE_ENV=production vite build --mode {args.appEnv}"
          }
        ]
      }
    }
  },
  "tags": []
}
